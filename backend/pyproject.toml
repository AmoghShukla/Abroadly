[project]
name = "abroadly-api"
version = "0.1.0"
description = "AI-assisted Master's abroad planning platform"
requires-python = ">=3.12"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]

